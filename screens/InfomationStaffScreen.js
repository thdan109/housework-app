import React from 'react'
import { View, Text, Dimensions} from 'react-native' 

const {width, height} = Dimensions.get('screen')

const InforStaff = ({navigation}) =>{

   return(
      <View>
         
      </View>
   )
}